<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.buffalo.cse.phonelab.phonelabservices"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="10" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name" android:debuggable="true">
        <activity
            android:label="@string/app_name"
            android:name=".PhonelabServicesActivity" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service android:name=".PhonelabController" android:process="@string/phonelab_controller_process"></service>
        <service android:name=".StatusMonitor" android:process="@string/status_monitor_process"></service>
        <service android:name=".DataLogger" android:process="@string/data_logger_process"></service>
    </application>

</manifest>